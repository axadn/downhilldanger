import * as MathUtils from "./math_utils";

const boxIntersectsBox = (matrix0, dimensions0, matrix1, dimensions1) =>{
  box1 points = [];
  for(let x = -1; x <= 1; x+= 2){
    for(let y = -1; y<= 1; y+= 2){
      for(let z = -1; z<= 1; z+= 2){

      }
    }
  }
}
